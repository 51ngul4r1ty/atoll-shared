name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Transpile
      run: |
        echo tranpiling...
        npm ci
        npm run transpile
