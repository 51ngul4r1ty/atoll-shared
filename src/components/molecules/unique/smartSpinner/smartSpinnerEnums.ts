export enum SpinnerAction {
    Loading = 1,
    Calculating = 2
}

export enum SpinnerSize {
    Default = 0,
    Small = 1,
    Medium = 2,
    Large = 3
}

export enum SpinnerTextPosition {
    OverSpinner = 1,
    BelowSpinner = 2
}
