// externals
import * as React from "react";

// components
import { HomeButton } from "../buttons/HomeButton";

/* exported components */

export const TopMenuPanel: React.FC<{}> = () => (
    <div>
        <HomeButton />
    </div>
);
